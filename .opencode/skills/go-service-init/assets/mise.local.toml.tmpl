# Local-only environment overrides. NEVER commit this file.
# It is added to .gitignore automatically.

[env]
# Example:
# DATABASE_URL = "postgres://postgres:postgres@localhost:5432/{{SERVICE_NAME}}?sslmode=disable"