//go:build wireinject
// +build wireinject

package main

import (
	"github.com/{{MODULE_PATH}}/internal/config"
	// Wire imports are added as the app, transport, and infra packages are created.
)

// wireApp builds the dependency graph and returns a wired App.
// Calls generator when `mise gen:wire` runs; do not edit this function.
//func wireApp(cfg *config.Config) (*App, func(), error) {
//	panic(wire.Build(
// Provide sets here as packages are implemented.
//	))
//	return nil, nil, nil
//}